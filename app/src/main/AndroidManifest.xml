<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.candr.test_uncore2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme">
        <activity android:name=".Login.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login.Register"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Login.parentLogin"></activity>
        <activity android:name=".Login.teacherLogin"></activity>
        <activity android:name=".Login.studentLogin"></activity>
        <activity android:name=".Login.staffLogin"></activity>
        <activity android:name=".content_main.result"></activity>
    </application>

</manifest>